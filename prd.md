
# 老年人电视播放器 Android TV App - 产品需求文档

## 1. 产品概述

### 1.1 产品定位
为老年人设计的简洁易用的Android TV本地视频播放器，专注于本地/外接存储设备的电视剧播放功能。

### 1.2 目标用户
- 55岁以上老年人群体
- 需要观看本地存储电视剧内容的用户
- 不熟悉复杂操作的用户

### 1.3 产品目标
- 操作简单，学习成本低
- 遥控器操作流畅，无需鼠标键盘
- 稳定可靠的播放体验
- 智能记忆播放进度

## 2. 功能需求

### 2.1 遥控器操控（P0）

#### 2.1.1 基础按键支持
| 按键 | 功能 |
|------|------|
| 方向键（上下左右） | 菜单导航、焦点移动 |
| 确认键（OK/Enter） | 短按：选择确认、播放/暂停<br>长按：倍速播放 |
| 返回键（Back） | 返回上一级、退出播放 |
| 主页键（Home） | 返回App首页 |

#### 2.1.2 播放控制按键
| 按键 | 功能 |
|------|------|
| 播放/暂停 | 控制视频播放状态 |
| 快进键 | 快进10秒（短按）/30秒（长按） |
| 快退键 | 快退10秒（短按）/30秒（长按） |
| 左右方向键（播放中） | 替代快进快退功能 |
| OK键长按 | 倍速播放（循环切换：1.0x → 1.25x → 1.5x → 2.0x → 1.0x） |

#### 2.1.3 倍速播放功能
- **触发方式**：播放界面长按OK键（持续1秒）
- **倍速档位**：1.0x（正常）、1.25x、1.5x、2.0x
- **循环切换**：每次长按OK键切换到下一档位，2.0x后回到1.0x
- **视觉反馈**：
  - 屏幕右上角显示当前倍速（如："1.5x"）
  - 字体大小：32sp，颜色：半透明白色背景 + 黄色文字
  - 显示3秒后自动隐藏
- **音频处理**：保持音调不变（使用ExoPlayer的变速不变调功能）
- **状态保持**：倍速设置在当前播放会话内保持，退出播放后重置为1.0x

#### 2.1.4 焦点设计规范
- **大尺寸焦点框**：边框宽度≥4dp，使用高对比度颜色（建议：亮黄色 #FFD700）
- **焦点动画**：缩放效果（1.0x → 1.1x），时长200ms
- **焦点音效**：焦点移动时播放轻微提示音
- **焦点记忆**：返回页面时，焦点停留在上次位置

### 2.2 扫描目录管理（P0）

#### 2.2.1 扫描目录添加
- **入口位置**：设置页面 → "管理扫描目录"
- **添加方式**：
  - 显示设备所有存储位置（内部存储、USB设备、外接硬盘）
  - 用户浏览文件夹树形结构
  - 选择目标文件夹后，点击"添加到扫描列表"
- **目录限制**：
  - 最多添加20个扫描目录
  - 不允许添加嵌套目录（子目录已在扫描列表中，禁止添加父目录）

#### 2.2.2 扫描规则
- **扫描深度**：仅扫描一级目录（不递归扫描子目录）
- **资源单位**：每个一级目录视为一个独立的电视剧资源
- **示例结构**：
  ```
  /storage/外接硬盘/电视剧/           ← 扫描目录
      ├─ 西游记/                     ← 识别为1个电视剧资源
      │   ├─ 第01集.mp4
      │   ├─ 第02集.mp4
      │   └─ ...
      ├─ 三国演义/                    ← 识别为1个电视剧资源
      │   ├─ EP01.mkv
      │   ├─ EP02.mkv
      │   └─ ...
      └─ 红楼梦/                      ← 识别为1个电视剧资源
          └─ 红楼梦全集.mp4          ← 单文件也视为1个资源
  ```

#### 2.2.3 文件识别规则
- **视频文件格式**：.mp4、.mkv、.avi、.mov、.flv、.ts、.rmvb
- **剧集排序规则**：
  - 优先识别文件名中的数字（01、02、E01、EP01、第1集等）
  - 按数字升序排序
  - 无法识别数字的，按文件名字母顺序排序
- **忽略规则**：
  - 文件大小 < 10MB
  - 隐藏文件（以"."开头）
  - 非视频格式文件

#### 2.2.4 扫描目录管理界面
```
管理扫描目录

已添加目录：
┌─────────────────────────────────────────┐
│ /storage/emulated/0/电视剧/             │ [删除]
│ /storage/USB1/我的视频/                 │ [删除]
│ /storage/USB2/连续剧/                   │ [删除]
└─────────────────────────────────────────┘

[+ 添加新目录]  [重新扫描所有目录]  [返回]
```

#### 2.2.5 扫描触发时机
- App首次启动时，提示用户添加扫描目录
- 添加新目录后立即扫描
- 用户手动点击"重新扫描所有目录"
- 检测到USB设备插入时，弹出提示询问是否添加该设备目录

#### 2.2.6 扫描进度反馈
- 显示进度对话框："正在扫描：/storage/USB1/电视剧/ (3/8)"
- 显示已发现资源数量："已找到 15 部电视剧"
- 扫描完成后显示结果："扫描完成，共发现 23 部电视剧"

### 2.3 文件夹缩略图（P0）

#### 2.3.1 缩略图生成规则
- **优先级1**：读取文件夹内的图片文件（poster.jpg、cover.jpg、folder.jpg）
- **优先级2**：提取文件夹内第一个视频文件的第1帧作为缩略图
- **优先级3**：使用默认占位图（胶片图标 + 电视剧名称首字）

#### 2.3.2 缩略图尺寸与缓存
- **显示尺寸**：宽度120dp × 高度160dp（3:4比例）
- **缓存策略**：
  - 生成后保存到本地缓存目录（/data/data/包名/cache/thumbnails/）
  - 使用文件夹路径MD5作为缓存文件名
  - 缓存大小限制：最多500MB，超出时删除最旧的缓存
- **异步加载**：使用Glide或Coil库异步加载，避免阻塞UI

#### 2.3.3 缩略图更新机制
- 文件夹内容变化时（新增/删除视频），自动重新生成缩略图
- 用户可在文件夹长按菜单中选择"刷新缩略图"

### 2.4 树状结构首页（P0）

#### 2.4.1 布局结构
```
┌────────────────────────────────────────────────────────────┐
│  电视剧                                          [设置]    │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  ┌────┐                                                   │
│  │缩略│ 西游记（25集）                                    │
│  │图  │                                                   │
│  └────┘ 继续观看：第8集  ████████░░░░  15:23/42:10       │
│         [第1集] [第2集] [第3集] [第4集] [第5集] [第6集]   │
│         [第7集] [第8集] [第9集] [第10集] [第11集] ...     │
│                                                            │
│  ┌────┐                                                   │
│  │缩略│ 三国演义（84集）                                  │
│  │图  │                                                   │
│  └────┘ [第1集] [第2集] [第3集] [第4集] [第5集] [第6集]   │
│         [第7集] [第8集] [第9集] [第10集] [第11集] ...     │
│                                                            │
│  ┌────┐                                                   │
│  │缩略│ 红楼梦（36集）                                    │
│  │图  │                                                   │
│  └────┘ 继续观看：第15集  ██████░░░░░░  22:45/45:30      │
│         [第1集] [第2集] [第3集] [第4集] [第5集] [第6集]   │
│         [第7集] [第8集] [第9集] [第10集] [第11集] ...     │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

#### 2.4.2 树状展示规则
- **始终展开**：所有电视剧资源默认展开，无折叠功能
- **垂直滚动**：整个页面上下滚动查看所有电视剧
- **单元结构**：每个电视剧占据一个完整的展示单元

#### 2.4.3 单个电视剧单元布局
```
┌────────────────────────────────────────────────────────────┐
│ [缩略图]  电视剧名称（总集数）                             │
│ 120x160   字体：28sp，颜色：#FFFFFF                        │
│           ↓                                                │
│           继续观看区域（如果有历史记录）                   │
│           第X集  进度条████░░░░  时间/总时长               │
│           高度：60dp，背景：半透明深色，圆角：8dp          │
│           ↓                                                │
│           选集列表（自动折行）                             │
│           [第1集] [第2集] [第3集] [第4集] [第5集] ...     │
│           [第7集] [第8集] [第9集] [第10集] [第11集] ...   │
│           按钮尺寸：140dp × 60dp，间距：12dp               │
└────────────────────────────────────────────────────────────┘
```

#### 2.4.4 继续观看区域
- **显示条件**：该电视剧存在未播完的记录（播放进度 < 95%）
- **显示内容**：
  - 左侧：集数文字（如："第8集"，字体：24sp）
  - 中间：进度条（宽度：300dp，高度：8dp，圆角：4dp）
  - 右侧：时间文字（如："15:23 / 42:10"，字体：20sp）
- **背景样式**：半透明深色背景（#80000000），圆角8dp，内边距16dp
- **位置**：电视剧名称下方、选集列表上方
- **可聚焦**：该区域可接收焦点，点击OK键直接续播

#### 2.4.5 选集列表布局
- **不做横向滚动**：按钮自动折行显示
- **按钮尺寸**：宽度140dp × 高度60dp
- **按钮间距**：水平12dp，垂直12dp
- **按钮样式**：
  - 未播放：深灰色背景（#424242），白色文字
  - 已播放：浅灰色背景（#616161），白色文字
  - 播放中：黄色边框（#FFD700），深色背景，白色文字
  - 焦点状态：缩放1.1倍 + 黄色高亮边框
- **折行规则**：
  - 1080p屏幕：每行最多显示12个按钮
  - 2160p屏幕：每行最多显示24个按钮
  - 根据屏幕宽度动态计算每行按钮数量

#### 2.4.6 焦点导航逻辑
- **上下键**：在不同电视剧单元之间切换
  - 向下：移动到下一个电视剧的继续观看区域（如有）或第一个选集按钮
  - 向上：移动到上一个电视剧的选集按钮或继续观看区域
- **左右键**：在同一电视剧的选集按钮之间移动
  - 到达行尾：自动跳到下一行
  - 到达行首：自动跳到上一行
- **边界处理**：
  - 第一个电视剧向上：焦点移动到顶部菜单栏
  - 最后一个电视剧向下：保持在最后一个选集按钮

#### 2.4.7 双击播放功能
- **作用对象**：缩略图区域
- **触发条件**：在缩略图上连续按下两次OK键，间隔 ≤ 500ms
- **单击行为**：焦点移动到该电视剧的继续观看区域或第一个选集按钮
- **双击行为**：直接从第1集第0秒开始播放，忽略历史记录

#### 2.4.8 长文本处理
- 电视剧名称超过20个字符时，截断并显示省略号
- 缩略图区域获得焦点时，以跑马灯形式完整显示名称
- 跑马灯速度：30dp/秒

### 2.5 续播功能（P0）

#### 2.5.1 播放进度记忆
- 自动保存播放进度（每5秒更新一次）
- 记录字段：
  - 电视剧文件夹路径
  - 当前播放的视频文件路径
  - 播放位置（毫秒）
  - 视频总时长
  - 最后播放时间

#### 2.5.2 续播触发场景
| 场景 | 行为 |
|------|------|
| 播放中途退出App | 保存进度，首页显示继续观看区域 |
| 播放完成（播放进度>95%） | 标记为已完成，不显示继续观看区域 |
| 正常播放结束 | 自动播放下一集（如果存在） |
| 手动选择新集数 | 覆盖旧的播放记录 |
| 双击缩略图 | 清除播放记录，从第1集开始 |

#### 2.5.3 继续观看交互
- 焦点停留在继续观看区域时，按下OK键直接跳转到上次播放位置
- 继续观看区域在焦点导航中优先级高于选集按钮
- 从其他电视剧单元向下导航时，优先停留在继续观看区域（如有）

#### 2.5.4 数据存储
- 使用本地数据库（Room/SQLite）
- 每个电视剧资源保存一条播放记录
- 提供"清除播放历史"功能（位于设置页面）

### 2.6 快进快退（P0）

#### 2.6.1 快进快退档位
| 操作 | 跳转时长 |
|------|----------|
| 短按方向键左/右 | 10秒 |
| 长按方向键左/右（1秒） | 30秒 |
| 长按方向键左/右（3秒） | 进入拖动模式（每次方向键按下跳转1分钟） |

#### 2.6.2 视觉反馈
- 显示浮层提示："快进 +10秒" / "快退 -10秒"
- 显示当前播放进度条（进度条高度≥8dp）
- 显示当前时间 / 总时长（如："15:23 / 42:10"）
- 提示3秒后自动隐藏

#### 2.6.3 边界处理
- 快退至起点（00:00）时，停留在开头
- 快进至终点时，停留在最后一帧（不自动跳转下一集）

### 2.7 设置页面（P0）

#### 2.7.1 设置项列表
```
设置
├─ 管理扫描目录
│   ├─ 添加目录
│   ├─ 删除目录
│   └─ 重新扫描所有目录
├─ 清除播放历史
├─ 字幕设置
│   ├─ 字幕大小（小/中/大/超大）
│   └─ 字幕颜色（白色/黄色/绿色）
├─ 播放设置
│   ├─ 快进快退时长（5秒/10秒/30秒）
│   └─ 自动续播（开/关）
├─ 关于
│   ├─ 版本号
│   └─ 使用帮助
└─ 返回首页
```

#### 2.7.2 操作反馈
- 每个设置项修改后，显示Toast提示"设置已保存"
- 扫描目录时，显示进度对话框
- 清除历史时，弹出二次确认对话框："确定清除所有播放历史？此操作不可恢复"

## 3. 非功能需求

### 3.1 性能要求
- App启动时间：≤3秒
- 文件扫描速度：≥100个文件/秒（一级目录）
- 播放启动时间：≤2秒
- 内存占用：≤200MB
- 4K视频流畅播放（30fps以上）
- 缩略图生成时间：≤1秒/张
- 长列表滚动流畅性：60fps

### 3.2 兼容性要求
- Android TV 5.0（API 21）及以上
- 支持1920x1080、3840x2160分辨率
- 支持ARM、ARM64架构（**不支持x86架构**）

### 3.3 可访问性
- 所有UI元素支持焦点导航
- 字体大小≥24sp
- 颜色对比度符合WCAG AA标准
- **不支持语音辅助（TalkBack）**

### 3.4 稳定性
- 崩溃率：≤0.1%
- ANR率：≤0.05%
- 播放错误自动恢复机制
- USB设备异常拔出时的容错处理

## 4. 技术约束

### 4.1 开发环境
- 开发语言：Kotlin
- 最低SDK版本：21（Android 5.0）
- 目标SDK版本：34（Android 14）

### 4.2 关键技术栈
- **UI框架**：Leanback库（Android TV官方UI组件）或 RecyclerView
- **播放器**：ExoPlayer 2.x（支持变速播放、多格式）
- **数据库**：Room（播放历史存储）
- **图片加载**：Glide或Coil（缩略图异步加载与缓存）
- **权限**：READ_EXTERNAL_STORAGE（必须）

### 4.3 第三方库限制
- 禁止使用任何网络库（Retrofit、OkHttp等）
- 禁止使用数据统计SDK
- 禁止使用广告SDK

### 4.4 存储权限处理
- Android 6.0+：运行时动态申请存储权限
- Android 10+：使用 `requestLegacyExternalStorage=true` 兼容旧版存储方式
- Android 11+：申请 `MANAGE_EXTERNAL_STORAGE` 权限（用户可能需要在系统设置中手动授权）

### 4.5 长列表优化
- 使用RecyclerView + ViewHolder模式
- 实现视图复用，避免重复创建视图
- 缩略图懒加载，仅加载可见区域的缩略图
- 选集按钮使用FlexboxLayoutManager实现自动折行

## 5. 用户体验设计原则

### 5.1 老年人友好设计
- **大字体**：所有文字≥24sp
- **高对比度**：文字与背景对比度≥7:1
- **简化操作**：核心功能3步以内完成
- **明确反馈**：每个操作都有视觉或音效反馈
- **容错设计**：避免误操作，重要操作需二次确认

### 5.2 操作流畅性
- 所有焦点移动响应时间≤100ms
- 页面滚动动画时长≤300ms
- 避免复杂手势，仅使用方向键+确认键
- 双击检测时间窗口500ms，确保识别准确
- 长列表滚动优化，避免卡顿

### 5.3 视觉设计
- **主色调**：深色背景（#1A1A1A）+ 亮色文字（#FFFFFF）
- **焦点色**：亮黄色（#FFD700）
- **卡片圆角**：8dp
- **阴影效果**：焦点元素增加8dp elevation
- **缩略图占位图**：使用Material Design风格的胶片图标
- **继续观看区域**：半透明深色背景（#80000000）突出显示

## 6. 数据结构设计

### 6.1 扫描目录表（scan_directories）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键 |
| path | TEXT | 目录绝对路径 |
| storage_type | TEXT | 存储类型（internal/usb/external） |
| added_time | INTEGER | 添加时间戳 |

### 6.2 电视剧资源表（tv_series）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键 |
| folder_path | TEXT | 文件夹绝对路径 |
| name | TEXT | 电视剧名称（文件夹名） |
| thumbnail_path | TEXT | 缩略图缓存路径 |
| episode_count | INTEGER | 总集数 |
| scan_directory_id | INTEGER | 所属扫描目录ID |
| last_scan_time | INTEGER | 最后扫描时间戳 |

### 6.3 剧集表（episodes）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键 |
| tv_series_id | INTEGER | 所属电视剧ID |
| file_path | TEXT | 视频文件绝对路径 |
| file_name | TEXT | 文件名 |
| episode_number | INTEGER | 集数（解析后的序号） |
| duration | INTEGER | 视频时长（毫秒） |
| file_size | INTEGER | 文件大小（字节） |

### 6.4 播放历史表（play_history）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键 |
| tv_series_id | INTEGER | 电视剧ID |
| episode_id | INTEGER | 当前播放的剧集ID |
| position | INTEGER | 播放位置（毫秒） |
| duration | INTEGER | 视频总时长（毫秒） |
| progress | REAL | 播放进度（0.0-1.0） |
| last_play_time | INTEGER | 最后播放时间戳 |

## 7. 交付物

### 7.1 开发阶段交付
- [ ] UI设计稿（Figma/Sketch）
- [ ] 技术方案文档
- [ ] 数据库设计文档
- [ ] 测试用例文档

### 7.2 上线交付
- [ ] APK安装包（ARM、ARM64版本）
- [ ] 用户使用手册（大字版 + 图文教程）
- [ ] 测试报告
- [ ] 源代码及文档

## 8. 测试要点

### 8.1 功能测试
- ✓ 遥控器所有按键功能测试
- ✓ 双击检测准确性测试（500ms时间窗口）
- ✓ 倍速播放切换测试（1.0x - 2.0x）
- ✓ 各种视频格式播放测试
- ✓ 续播功能各场景测试
- ✓ 快进快退边界测试
- ✓ 外接设备热插拔测试
- ✓ 扫描目录添加/删除测试
- ✓ 一级目录扫描准确性测试
- ✓ 缩略图生成与缓存测试
- ✓ 继续观看区域显示逻辑测试
- ✓ 选集列表折行显示测试
- ✓ 树状结构焦点导航测试

### 8.2 性能测试
- ✓ 100+电视剧展开滚动流畅性测试
- ✓ 单个电视剧100+集数折行显示测试
- ✓ 长列表滚动帧率测试（目标：60fps）
- ✓ 缩略图懒加载测试
- ✓ 内存占用测试（展开100+电视剧）

### 8.3 老年人用户测试
- ✓ 首次使用学习时长（目标：≤5分钟）
- ✓ 核心功能完成率（目标：≥90%）
- ✓ 双击操作成功率（目标：≥85%）
- ✓ 焦点导航易用性评估
- ✓ 用户满意度调研

### 8.4 压力测试
- ✓ 100+文件夹扫描测试
- ✓ 单个文件夹100+集数显示测试
- ✓ 长时间播放稳定性测试（≥4小时）
- ✓ 低存储空间下的运行测试
- ✓ 缩略图缓存上限测试（500MB）

### 8.5 边界测试
- ✓ 空文件夹处理
- ✓ 无视频文件的文件夹处理
- ✓ 视频文件名无法解析集数时的排序
- ✓ USB设备扫描中途拔出
- ✓ 播放中USB设备拔出
- ✓ 权限拒绝时的提示与引导
- ✓ 极长电视剧名称显示测试

## 9. 里程碑计划

| 阶段 | 交付内容 | 时长 |
|------|----------|------|
| 需求评审 | PRD确认 | 1周 |
| UI设计 | 树状结构页面设计稿 | 2周 |
| 开发阶段1 | 扫描目录管理+文件扫描 | 2周 |
| 开发阶段2 | 树状首页+缩略图生成 | 3周 |
| 开发阶段3 | 选集列表折行+焦点导航 | 2周 |
| 开发阶段4 | 播放功能+倍速播放 | 2周 |
| 开发阶段5 | 续播+继续观看区域+快进快退 | 2周 |
| 开发阶段6 | 双击播放+设置页面 | 1周 |
| 优化阶段 | 长列表性能优化+细节打磨 | 2周 |
| 测试阶段 | 完整测试+修复 | 2周 |
| 用户测试 | 老年人群体试用 | 1周 |
| 发布准备 | 文档+打包 | 1周 |

**总计**：21周

## 10. 风险评估

| 风险 | 影响 | 应对措施 |
|------|------|----------|
| 长列表性能问题 | 高 | 使用RecyclerView视图复用+缩略图懒加载 |
| 选集按钮过多导致布局混乱 | 中 | 使用FlexboxLayoutManager自动折行 |
| 焦点导航复杂度高 | 高 | 提前设计焦点跳转逻辑，充分测试 |
| 外接硬盘格式兼容性问题 | 高 | 提前测试主流硬盘格式，提供格式化指引 |
| 缩略图生成耗时过长 | 中 | 使用异步队列生成，优先显示默认占位图 |
---

## 11. 开发环境与公约
1. 本机已安装Android Studio，可调起AVD进行测试
2. prd不是铁律，以完成应用特性为目标，可自行修改实现方式